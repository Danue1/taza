[package]
name = "taza-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "taza"
path = "src/main.rs"

[dependencies]
taza-engine = { workspace = true, features = ["lang-hangul"] }
taza-pack.workspace = true
taza-packbuild.workspace = true
