[package]
name = "taza-evaluation"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
taza-engine = { workspace = true, features = ["lang-latin", "lang-hangul"] }

[dev-dependencies]
taza-pack.workspace = true
