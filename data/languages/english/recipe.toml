# 영어 언어팩 — 앱에 내장되는 기본 언어라 크기 예산을 먼저 본다.
#
# 어휘 인벤토리는 SCOWL(관대한 커스텀 라이선스), 실사용 빈도는 Tatoeba(CC-BY 2.0 FR).
# 둘 다 저작자 표시만 요구하므로 share-alike가 파생 팩에 옮겨붙지 않는다.
# 대문자 목록(upper·proper-names)은 넣지 않는다 — 조회가 대소문자를 구분하므로 v1
# 라틴 경로에서는 소문자 표제어만 쓸모가 있다.

# 팩이 스스로 밝히는 것 — 여기 적힌 것은 전부 팩 메타데이터로 기기에 나간다.
# 표시 이름·키캡 표기·조합 골격도 팩이 밝힌다 — 코어와 셸은 자기 표를 두지 않는다.
[pack]
language = "en"
display_name = "English"
keycap_label = "A"
composer_skeleton = "latin"
pack_version = 3

[pack.script]
word_separated = true
right_to_left = false

# 여기서만 쓰는 손잡이 — 값은 나가지 않고, 값이 고른 결과만 나간다.
[build.lexicon]
encoding = "utf8"
character_set = "latin-lowercase"
max_words = 80000
minimum_word_length = 2

# 내장 팩이라 문맥 신호도 크기 예산 안에서 고른다 — 이득 상위만 담는다.
[build.language_model]
max_bigrams = 25000
minimum_count = 5
